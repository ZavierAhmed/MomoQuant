# MOMO Quant Docs Package

Place this `/docs` folder at the root of the MOMO Quant project before asking Cursor to implement anything.

Start with `10-cursor-handoff-guide.md`, then use the milestone prompts and sequence in `08-cursor-implementation-plan.md`.

Do not ask Cursor to build the whole app in one prompt. Build milestone by milestone.

Additional ops notes:

- `11-local-secrets-and-hosting-security.md` — local user-secrets / env vars after rotation, and hosting security blockers.
